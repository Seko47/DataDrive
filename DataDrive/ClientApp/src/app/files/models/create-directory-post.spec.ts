import { CreateDirectoryPost } from './create-directory-post';

describe('CreateDirectoryPost', () => {
  it('should create an instance', () => {
    expect(new CreateDirectoryPost()).toBeTruthy();
  });
});
